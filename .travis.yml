language: ruby
rvm:
  - 2.3
script: "bundle exec jekyll build"
deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: _site/
  keep_history: true
  on:
    branch: main
